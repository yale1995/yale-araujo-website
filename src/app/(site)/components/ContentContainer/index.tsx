import { ContentContainerAction } from './ContentContainerAction'
import { ContentContainerRoot } from './ContentContainerRoot'

export const ContentContainer = {
  Root: ContentContainerRoot,
  Action: ContentContainerAction,
}
